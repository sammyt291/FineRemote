{
  "name": "fine-remote",
  "version": "0.1.0",
  "private": true,
  "description": "A low-latency PeerJS remote desktop concept",
  "scripts": {
    "start": "node server.js",
    "client": "node server.js --client",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/ffmpeg.js && node --check electron-main.js && node --check electron-preload.js && node --check public/peer-session.js && node --check public/app.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "electron": "^44.3.0",
    "express": "^5.1.0",
    "ffmpeg-static": "^5.3.0",
    "peer": "^1.0.2",
    "peerjs": "^1.5.5"
  }
}
